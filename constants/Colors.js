import { Platform } from 'react-native';

// Platform.OS === 'android'
const Colors = {
  fonts: {
    primary: "#000",
    alternate: "#fff",
    bold: "bold"
  },
  backgrounds: {
    primary: "purple",
    secondary: "green",
    third: "blue",
    alternate: "#fff"
  }
};

export default Colors;